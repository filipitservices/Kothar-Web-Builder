# Sidebar Branding (Removed)

**SidebarBranding.vue** has been removed from the builder. Account and help functionality are available via **UserMenu** in the AppNavbar on pages that use the default layout (dashboard, sites, etc.). The builder uses the builder layout, which has no navbar, so account actions are accessed by navigating to the dashboard or other authenticated pages.
